/**
 * Firebase removed - auth now uses Turso + NextAuth only
 * Credentials provider with email/password stored in Turso
 */

export const app = null;
export const auth = null;
export const db = null;
export const isConfigured = false;
